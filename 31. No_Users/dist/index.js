"use strict";
let userName = ["admin", "Rija", "Aina", "Wania", "Waleeja"];
if (userName.length === 0) {
    console.log("We need to find some users!");
}
else {
    userName = [];
    console.log("All userd names have been removed." + userName.length);
}
